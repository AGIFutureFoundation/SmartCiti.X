# SmartCiti.X : Trade Craft Academy — implementation bundle v3.2

*powered by AGI Corp*

Gamified training to enhance robotic and human integrations.
The Adaptive Stack: the packs implementing the ACP protocol suite (v3.2) —
counted, like everything else here, by the table below rather than by a
number that can drift from it.

**2,227 checks, all passing from inside this bundle**, with no model credentials
required.

```bash
./verify_all.sh                    # everything below, one exit code
```

Or individually — every path inside a pack resolves against its own module
location, so these run from any working directory (v2.6 defect 13):

```bash
node unions/verify.mjs             # 28 — roster, districts, campuses, chapters, pack agreement, and the interactive map's chapter-seats chip (every hall, home marked, never a link)
node pack/verify.mjs               # 29 — the registry, all 11,000,000 IDs, and per-hall practitioner sign-off (pack/hall_signoff.mjs)
node control/test.mjs              # 12 — learner profile, ZPD dial, affect
node control/test_graph.mjs        # 20 — skill graph, sequencer, gates
node control/test_hints.mjs        # 24 — the hint ladder, fading, the ceiling contract, an unreported dwell is refused
node fabric/test.mjs               # 16 — mentor guards, eval harness, router
node bus/test.mjs                  # 17 — bus contracts, telemetry guards, audit
node bus/test_safeguards.mjs       # 20 — parity (including the §23.1 "measured nothing" fail-open), stop conditions, overrides
node security/test.mjs             # 29 — authz, tenancy, rate limits, privacy, the contest and abuse routes, SECURITY.md held to the packs
node security/test_sbom.mjs        # 11 — the SBOM: every one of the 41 files under `web/vendor/` hashed, versioned and licensed from evidence in the file — or, for the 26 self-hosted font faces, from the exact URL the fetcher recorded and the full SIL OFL committed beside them, each family its own component under its own copyright; the two files this bundle generated there are listed as first-party and forbidden to claim an upstream
node ops/test.mjs                  # 48 — registries, rollout lanes (an unreported dwell opens nothing, a rollout manager refuses to run with no audit log), jobs, cost governor
node ops/fuzz.mjs                  #  9 — side doors around the promotion gates
node ops/rehearsal.mjs             # 15 — the wave 1 rehearsal: one content rollout walked through every ACP-13/ACP-08 gate against seeded synthetic cohorts (dwell, parity, halt, auto-rollback, the dial-params shadow run, a self-halting parity job driven through the real Scheduler, the full canary→wave→full walk) with the complete audit trail asserted, not just return values — proves the machinery, not that a wave shipped to real halls
node brand/test.mjs                # 30 — naming, tokens, contrast, livery, the lint itself
node brand/lint.mjs .              # fails on any forbidden spelling anywhere in the tree
node i18n/test.mjs                 # 31 — locale parity, placeholders, pack agreement, the reviewer-attribution rule (a closed status set, a `reviewed` catalog must name its reviewer, a hall-name catalog complete or absent never partial) with self-tested fixtures
node i18n/lint_hardcoded.mjs       # fails on any hard-coded English string in the landing, map or languages generators — self-tested against a fixture sentence, hard-coded alt text and a JS string literal
node stations/test.mjs             # 27 — recovered stations vs roster, skills, rooms
node surfaces/test.mjs             # 62 — finishes, walls, conditions, more-demanding-wins, honesty
node geo/test.mjs                  # 35 — real coordinates + anchors (RECORDED, citing Locator.X, near the three flagship campuses; AUTHORED, admittedly not cross-checked, near the seven hub campuses), recomputed distances, GeoJSON + the network map file
node sims/test.mjs                 # 55 — 11 simulators bound to 45 real halls (the boom lift and the overhead crane new, each with an in-headset control mapping), each with a scripted reference operator (3 levels, guarantees asserted against the rubric, procedure ids the page's policy is written around) (crane-lift/rigging-signals/load-chart extend to every trade that actually rigs, signs or plans a pick — ironworkers, decking, tank-erectors, piling; weld-bead to marine-pipe and tank-erectors; scaffold-bay to the rest of the envelope district plus refractory; pressure-washer to laborers and hazmat alongside painters), rubrics, cockpit + scoring + regional scenarios, and the interactive map's own simulator-seat badge, gated to bound halls only, deep-linked into the 3D app
node tools/test.mjs                # 13 — district tool cribs, the deterministic crib drill, hall bindings
node schools/test.mjs              # 22 — flipped-classroom model, proposed districts, live units + class drills, bidirectional Schools/hall panel links, and the interactive map's own flipped-unit badge, gated to halls with a unit only
node avatars/test.mjs              # 52 — locker depth, crews, characters, costumes, the TradeApes, marks policy, the guarantee, now on the network dashboard too
node parcels/test.mjs              # 25 — the city-records source contract: authorities, licences, frames, imagery, honesty, and the shared elevation/aerial ground-truth module (web/groundtruth.py) both the 3D app and the geomap inline
node roadmap/test.mjs              # 34 — the network roadmap: 10 built (3 district campuses + 7 regional-chapter hubs) + 0 candidates, totalling exactly 10 — the ten-campus target fully met, with the suite holding for an empty candidates table exactly as it held for a full one
node training/test.mjs             # 43 — the robotic training-data recorder: episode shapes, the storage/export contract, the not-scored guarantee, TRACE's own gauge-sampling add-on, the Orbis cross-link, the `actor` field and the SCRIPTED tier (never AI-SYNTHESIZED), the balanced-paren pass-expression guard
node orbis/test.mjs                # 68 — the Orbis synthetic-training prompt contract + the two real video-model runners, cross-linked from the panel itself in both directions and now on the network dashboard too: no key shipped, no network reached, AI-SYNTHESIZED labelled
node restoration/test.mjs          # 84 — the Bay Restoration pack: 11 real sites across two categories — 9 independently-run sfbayrestore.org habitat-restoration sites (8 mapped) and two environmental-monitoring sites, Hunters Point Naval Shipyard, a real, currently-litigated federal Superfund cleanup site, and Former Naval Station Treasure Island (NSTI), a real Navy BRAC cleanup that is not NPL-listed, on the island the flagship campus scene itself sits on (both pinned but deliberately never walkable) — and 3 field-skill tracks bound to real skill_ids, on the geomap, on the network dashboard, and in a live 3D panel with a bidirectional hall link, not affiliated with or certified by this bundle; every site also names its real trade_needs (real union slugs whose own registered focus actually matches that site's field work — never forced). Those 8 habitat-restoration sites stand as real, clickable markers a learner can walk up to in the Treasure Island / Oakland city layer, and each opens onto its own standalone, walkable ground-level scene, terrain composed from that exact site's own real habitat/scale/org record — never one template stamped eight times — with three in-world field-skill-track beacons that still link straight to the real hall that teaches each one; each pinned site's own panel also offers an on-request real USGS 3DEP elevation reading and a real USGS National Map aerial-imagery thumbnail at its own coordinate, the same two live-fetched sources the city layer's own institution panels already use, kept a flat 2D panel result rather than blended into the deliberately schematic walkable ground. Hunters Point stays in that same flat-panel pattern — a located, clickable marker with real, individually-cited facts (a September 2026 snapshot, not a live feed) and a real community air-monitoring pathway (the Marie Harrison Bayview Air Monitoring Project / Greenaction / IVAN BVHP, not job training and not a SmartCiti.X program) — but never a walkable ground scene, because an actively litigated federal cleanup site is not something this platform can responsibly present as a place to stroll; NSTI holds to that same pattern — 21 individually-cited facts (a 17 September 2026 snapshot), an explicit not-NPL disambiguation against the Hunters Point Annex EPA ID, the 2022 resident class action stated as dismissed and never as settled, the real Navy-convened Restoration Advisory Board as oversight participation, and One Treasure Island's Construction Training Program as the island's real union pre-apprenticeship (not the cleanup, not a SmartCiti.X program) — while the walkable Treasure Island campus scene stays what it always was, a schematic training campus at the island's derived centroid, and the NSTI entry is the real-world record of the ground it stands on
node kit/test.mjs                  # 57 — **the modular exterior kit**: 18 pieces over 15 families (stoop, parapet, downpipe, vent, awning, bollard, rail, gutter, conduit, meter box, planter, cage ladder and the rest), median **34 triangles** each. Sized from measurement, not taste: `assets/REFERENCE.md` records that a residential block which reads convincingly as a place has a median mesh of 104 triangles. Every declared triangle budget is asserted against what its own primitive recipe costs — box 12, capped cylinder 4N, open cylinder 2N — in the builder and again independently in the suite. Placement is derived from roofline and facade, never typed per building: **no hall slug appears anywhere in the pack**, and the suite checks that against all 111. At the flagship campus, 51 halls carry **1,117 pieces and 31,392 triangles for 8 draw calls** — 140 pieces per call, against ceilings of 68 calls and 33,285 triangles both read out of `web/eval_scene.mjs` rather than chosen. The one-draw-call-per-piece anti-pattern is refused by arithmetic: a check requires ≥50 pieces per draw call, which a per-piece-mesh design can never reach
node props/test.mjs                # 70 — **the interior props**: 29 props over 7 families, median **96 triangles**, standing in all **11 of 11** room strands — 5,082 instances across the 1,221 rooms. A prop lands in a strand only if one of its declared tokens really appears in that strand’s own `purpose` string in `web/interiors.py`, and the suite re-derives every match from the module source. Triangle counts are not asserted against a formula but against the **vendored three.js**: the suite builds the real BoxGeometry and CylinderGeometry for every part of every prop and counts indices. Safety fixtures are derived from each room’s own `conditions.ppe` record — 888 rooms require PPE and the build fails if one of them gets no PPE prop, or if a PPE-free room gets one. Clearances come from the page’s own constants (0.45 m body radius, the 1.45 m partition top, the 2.2 m room sign), which caught five props mounted below the floor line on the first run. A hall pays **7–10 draw calls for 45 props**. Every safety prop repeats, in its own words, that a schematic shape is not a fixture specification, not a rated appliance and not evidence that any equipment has been provided
node lessons/test.mjs              # 63 — **the walkable lessons**: 32 lessons a learner can stand in, 135 steps across 8 closed kinds (walk, placard, station, crib, walkaround, sim, advisor, crew), each step resolving to a real id in another registry and asserted to exist. Breadth over depth by construction: **11 of 11 skill strands** and 27 of 111 halls, with a declared 10% ceiling on how much of the set any one hall may hold (the top hall holds 6.25%) and a build that fails if it is exceeded. 65 steps write one of the **4 episode kinds `training/` already declares and 0 new ones**; the other 70 carry an explicit null and a sentence saying why they record nothing. A 16-edge prerequisite ladder, 3 deep, proved acyclic by traversal rather than by construction, every edge naming a reason from a closed set that is itself re-derived from halls, seats and toolcribs. Nothing here is enforced: the page contract forbids locking a lesson whose prerequisites are unfinished. Every lesson carries its `limits`, and a clause-level scan fails the build on any certify/qualify/license claim without a denial in the same clause
node sky/test.mjs                  # 69 — **the day-night sky**: 14 phases over a solar day at the flagship campus’s own latitude (37.8235°, read from `geo/`, not retyped) — ten of them SOLVED by bisection from the elevation that *defines* the phase (−18° astronomical, −12° nautical, −6° civil, −0.833° for a risen disc allowing 16′ semidiameter and 34′ refraction), four constructed from the definition of apparent solar time; every sun elevation and azimuth DERIVED from Meeus 13.5/13.6 with Spencer’s declination series and re-derived independently by the suite, while every one of the 104 colours is AUTHORED and labelled as eye-chosen rather than computed from a Rayleigh integral; 6 weather skies holding 30 *pointers* into `world/` and copying none of it; 5 draw layers whose order is read off the page’s own anchors; 420 stars in 3 bins regenerated branch-for-branch from the page’s own seed, with the honest gap published (the drawn discs span 1.84 magnitudes; a real sky holding that many stars would reach 3.85). The page now places the sun, the key and hemisphere colours, their intensities and the star field by the selected hour; the gradient half is still unwired and the pack says which half is which
node respond/test.mjs              # 118 — **the first-responder and disaster-relief series**: 5 services — fire, law enforcement, EMS, emergency management, social work / crisis response — with 30 role tiers, 62 competency domains carrying 248 observable behaviours, and 25 scenario frames (17 of them three-or-more-agency) with 125 debrief questions. It is a **scaffold, not a protocol**: 23 standards bodies are named as *the authority that owns the real standard* — NFPA, NREMT, FEMA/ICS, state POST, NASW and others — and **0 of their 23 documents was opened by this build**, which has no network, so every authority record carries `document_read_by_this_build: false`, a null URL, and a `this_pack_reproduces` field beginning "none of it". No dose, no triage cut-off, no entry or restraint tactic: an 8-pattern safety scan runs inside the builder and is independently re-implemented in the suite, catching unit-bearing doses, local/lodge/apparatus numbers, a person behind a rank, named clinical agents, triage scores and any URL. 152 cross-links tie responder frames to 81 of the 111 existing trade halls, every slug resolved against the hall roster *and* the campus roster at build time, each carrying that hall's teaching-space PPE recomputed from `surfaces/` with a note that trade PPE is not responder PPE and does not transfer. The 30 untouched halls are listed by name. Four separate sign-off gaps rather than one, so a partial answer cannot hide: **0 of 117 items, 0 of 62 competencies, 0 of 25 frames, 0 of 30 role tiers, and 0 of 5 services reviewed by anyone serving in them**
node ei/test.mjs                   # 112 — **the emotional-intelligence layer**: what an advisor does when a learner is frustrated, ashamed of a mistake, frightened of a machine, burnt out, frozen out by a crew, or carrying a bad call. 59 records — 12 signals, 8 states, 12 responses, 11 red lines, 7 debrief moves, 9 advisor bindings — with the hard limit built in rather than disclaimed: every response carries a scope, an observable stop_condition, a resolving rung on the 5-rung handoff ladder (peer → journey-mentor → member-assistance → licensed professional → emergency services, each a resource TYPE) and a turn ceiling read from the spec's own Socratic bound, and the build refuses a response missing any of them. **No string in the payload is shaped like a phone number and no service is named** — a wrong number in a crisis is worse than none, and this build has no network to check one. Post-incident distress is hand-off-only: one turn, forbidden from asking what happened or offering a technique. The central invariant ties it to `fabric/`'s own measured finding — **no response may raise the served hint rung**, because an over-helping mentor measured a cohort lift of −0.063 against a −0.03 threshold and sympathy is not an exemption from that. Thresholds are all `null` with a stated reason rather than guesses in a calibration's clothes. **0 of 59 records has been read by any clinician, counsellor or member-assistance professional**, published as the first field of the honesty block
node auth/test.mjs                 # 50 — **sign-in, and what a bundle with no server cannot do**: 5 methods, of which 2 work and 3 are off. The two that work claim only what they are — a local identity that names whose training record this is on this device, and an EIP-4361 wallet signature. Both carry `authenticates: false` and `gates_anything: false`, the wallet one included, because nothing here can tell anyone apart. Google, Microsoft and email-link carry `configured: false` and 12 named missing server-side pieces between them; they still answer a click, on purpose, and the click runs the page's real writer, which throws with the adapter's own reason. Exactly one line in the whole page writes an identity record and it sits below that guard. The SIWE recovery is the page's own Keccak-f[1600] and secp256k1, lifted out of the shipped HTML and run here — held against oracles this pack did not write: the same sponge driven in SHA3-256 mode against node's `sha3-256`, the published keccak256(""), the published addresses of private keys 1, 2 and 3 with EIP-55 casing, and public keys node itself generated over secp256k1. The EIP-4361 field order is AUTHORED FROM RECOLLECTION — this build had no network, and a wallet that mis-renders the message is the signal, not a passing test
node xr/test.mjs                   # 40 — **the head-mounted target list**: the 15 AR/MR devices this bundle is aimed at, from 10 vendors, none of them RECORDED — every device fact is a vendor claim relayed as text, and all three vendor hosts sampled returned `000/ERR` from this build, so each entry carries its source URL and `claim_checked: false`. The field that matters fails closed: **2 of 15 name an eye-protection standard** (1 in the device itself, 1 via an attachable certified shield), and the other 13 are recorded as NOT replacing the eyewear a room requires — `null` means *this pack cannot say*, never *probably fine*. That is cross-referenced against the rooms this bundle already describes: of 1,250 rooms, 570 require eye protection and **228 require both eye and head protection**, which is where a headset is a third thing on a worker's head rather than a replacement for either. Helmet-attachment claims are graded 1–4 rather than flattened, because a vendor's named mount and "an integrator will work it out" are not the same claim, and a mount never inherits the helmet's rating. 2 devices carry a procurement flag this pack raised itself (a vendor believed defunct since 2019, a headset believed out of production), each stating its own confidence and that it is recollection rather than lookup. Naming hardware does not close `xr.no-headset-has-ever-run-this`, and the pack says so
node terrain/test.mjs              # 56 — **the real ground**: Natural Earth's own 10m coastline, land, lakes and rivers, fetched once by hand and committed with a hash manifest (clipped from 32,619,997 bytes to 152,384), turned into 8 coastline runs, 5 river runs and 3 lake rings in metres around each of the ten campuses, plus a land/water mask of 92,160 cells at 125 m each of which 21,694 are water. Every figure here is recomputed by the suite from the vendored GeoJSON — the mask by a *winding-number* test, where the builder used even-odd, so agreement means the answer is right rather than that the code was copied. Two findings the pack publishes rather than smooths: at 1:10,000,000 the mask puts 2 of the ten campus anchors (detroit, miami) on **water**, so the page draws the mask only outside the 520 m apron; and water is reported twice per campus, as a distance to a drawn line and a distance to a water cell, because Chicago has no water *line* in its window and a 40% water mask while New Orleans has the Mississippi 1.1 km away and a mask that is 100% land. No address, street or parcel is in here, and the pack says so: the Locator.X checkout that carries them keeps them out of git because several upstream feeds carry owner PII
node rnd/test.mjs                  # 61 — **the R&D register**: 74 measured facts over 11 files (11 in a browser, 51 elsewhere, 12 computed from a registry), each carrying a JSON or regex locator the suite re-resolves against the live file rather than trusting the stored value; 9 of 34 registries derived as declared-but-unbuilt from two independent signals; 17 gaps with numerator and denominator recounted from their own registries (widest: 0 of 111 halls signed off by a named practitioner); 7 budget figures cross-checked across draw calls and triangles in 3 views; and 12 open questions, 9 of them negative findings quoted verbatim between two unique anchors. Two cross-checks currently **disagree** and say so
node guide/test.mjs                # 56 — **the helper guide**: 11 places (6 world views, 4 panels and the front door) × 6 fixed asks = 66 topics, each answer citing the file it was written from; 63 control rows, of which 42 are read from `sims/registry/sims.json` verbatim rather than retyped; a voice policy whose two switches are OFF until switched on, each with a disclosure computed from its own six declared fields (speech-to-text sends recorded audio to the browser vendor’s service, said at the toggle rather than in a footnote, and the guide asks to recognise on-device where Chrome offers it and stops rather than falling back); and 4 hand gestures over 25 WebXR hand joints, every distance AUTHORED and labelled unverified on hardware
node spatial/test.mjs              # 45 — the spatial fabric: 67 OGC GeoPose 1.0 Basic-YPR poses (10 campuses, 47 anchors, 10 pinned restoration sites; the bay-wide Spartina program honestly unposed) held against their source registries pose by pose, height and heading zero-with-UNKNOWN on every one, the OMBI-shaped fabric manifest and SOM scene graph registered in meta/ as NOT claimed, every external operator a separate origin serving nothing through this fabric, and now the geomap too — every campus/anchor feature's own GeoPose ref joined at build time, the pose count and claimed/not-claimed split on the legend, and the same shared on-request elevation/aerial lookup the 3D app uses
node labels/test.mjs               # 39 — the signs: shape, colour and type per kind, and the field-of-vision focus
node world/test.mjs                # 45 — the sky, six weather states, the twenty-two generated ground recipes (one per campus, none shared) and the fauna
node agents/test.mjs               # 59 — the advisors and the crews: who stands where, quote-not-copy bindings, the closed book, the Operator's live-seat scoping and its quoted reference procedure
node venue/test.mjs                # 60 — **the measured venue**: a real multi-salon building (Maison de la Mutualite, Paris) read for the proportions a schematic grid has none of. The model itself is NOT in this tree and never enters the scene - 1,008 meshes is five times the hall's whole draw-call ceiling - and the suite walks the tree to prove no .glb, .gltf or .fbx is anywhere in it. The scan is CC-BY-4.0 by GL events Paris Venues, and the four things that licence asks for - author, a link to the work, a link to the deed, and an indication of changes - are structured fields with six checks holding them non-empty. The unit is published UNRESOLVED in model units rather than asserted in metres: the exporter's own 0.01 makes the building 19 cm across and undoing it gives a 1.35 storey, so the building is 13.8x wider than it is tall either way and no reading where one unit is one metre survives that. The ratios are the deliverable, because they transfer at any scale
node web/test_ladder.mjs           # 27 — **the union training ladder**: the 3,663-skill module graph connected to the simulator seats. Each union's own 33 cells (11 strands x 3 tiers), the prerequisite edges read from `requires` (3,552 of the 3,663 carry one), and where a seat stands in it via the 79 hall bindings, every one of whose skill_ids resolves. The number that matters is the one the page refuses to inflate: all 11 seats are `machines/applied`, so a seat stands on ONE of the 33 cells and ten strands — safety, tools, procedure, inspection, layout, materials, troubleshooting, coordination, documentation, leadership — carry no seat at all. The page draws the whole ladder with coverage marked rather than only the covered cell, and a check recomputes all 9 keyed figures from the registries that own them, so a coverage claim larger than the data supports fails the build
node web/test_3d.mjs               # 258 — the 3D environment's teardown, draw-call, surface and per-frame contracts, held at the generator’s source — including the hour of the day (the sun is placed along the selected phase’s own solved elevation and azimuth instead of one fixed vector shared by ten campuses forever; the light’s basis is rebuilt when it moves, the hour is applied before the sky is drawn, and the stars belong to the sun rather than to one weather state), shadows on every device (the touchscreen flag no longer decides whether a device gets them; the quality ladder, which measures real frame time, does — proven by feeding it synthetic frame rates: 24 fps keeps shadows, 21 fps loses them), the helper guide (one control reachable from every view and from over every panel, six fixed questions per place answered from `guide/`, a running seat’s controls resolved against the simulator registry rather than copied, both voice switches off until switched on, and four WebXR hand gestures whose hysteresis band and dwell are proven against synthetic joint positions), the sun, whose shadow box now rides with the view instead of standing as a fixed ±60 m box at the world origin that left a third of the campus in flat light behind a hard line, and the walker (a body that leans into a stride, is not faster on the diagonal, moves at speeds a person moves at, whose footsteps take their timbre from the floor or ground the registries already named, which cannot walk through a hall, and which indoors is stopped by the shell and by partitions that now carry doorways cut on the boundary between two rooms — every one of the 1,221 rooms still reachable on foot, proven) and the machine drives, where the four lever seats spool their axes up and coast them down instead of starting and stopping on the key, a loaded machine works slower than an empty one, the overhead crane’s upper limit switch cuts its own hoist-up circuit, and the weld seat’s arc length drives heat input rather than only being scored: region board and campus disposed (never just detached), label sprites pruned and their textures released, the restoration walk restoring the campus rather than rebuilding it, decoration pooled per district, beacons instanced, roads and dashes merged in campus coordinates, the sim dash written only on change; and the surface contracts — one engine serving floors and walls with a normal map read off the pattern's own height field, walls and per-room partitions read from the registry rather than one flat colour, the room's illuminance record driving a real light rather than an emissive box that lights nothing, those lights on the quality ladder, and a PPE placard hung only where a room actually requires PPE
node web/eval_scene.mjs            # opt-in, needs a browser and a local server — **the scene scorecard**. `verify_all.sh` opens no browser and reaches no network, which is right, but some questions cannot be answered from the source at all: what a view actually costs in draw calls, and whether enough separate things are standing in it for it to read as a place. This drives the page through region, campus and hall and scores each against ceilings set from a measured baseline plus stated headroom — 25% on draw calls, the scarce currency, and 4× on triangles, which are nearly free here and are meant to grow. Baseline 2026-09-22: region 249 calls / 4,508 triangles, campus 273 / 15,850, hall 157 / 3,806
node web/test_rtl.mjs              # 17 — RTL correctness held at the generator's source: the languages page's dir="rtl" switch now reaches <html> itself (not just the inner section), the physical left/right CSS a source read found in the landing and map generators replaced with logical properties, no other hard-coded direction rule survives either file — the actual-render half (Arabic tab, bounding boxes, overflow) is a scratch Playwright proof, reported but not committed
node meta/test.mjs                 # 34 — the metaverse layer: standards (now four, GeoPose 1.0 via spatial/), the OMBI shapes honestly not claimed, the Unity avatar review, the complete VRM skeleton and how it moves, import policy, honesty, now on the network dashboard too
node control/fuzz.mjs              # 21 — hostile inputs and adversarial learners
node control/soak.mjs              # long-run invariants, 4,000 attempts x 3 seeds
python3 console/check_console.py   # fails if the console is behind its sources
python3 wiki/build_wiki.py --check # fails if the wiki is behind the registries
python3 web/build_page.py --check        # fails if the spec page is behind its builder
python3 web/build_dashboard.py --check   # fails if the network dashboard is behind its builder
python3 web/build_interactive_map.py --check # fails if the interactive map is behind its builder
python3 web/build_map.py --check         # fails if the campus map is behind its builder
python3 web/build_geomap.py --check      # fails if the network geomap is behind its builder
python3 web/build_languages.py --check   # fails if the languages page is behind its builder
python3 web/build_landing.py --check     # fails if the landing page is behind its builder
python3 web/build_3d.py --check          # fails if the 3D environment is behind its builder
```

| Pack | Spec | What it is |
|---|---|---|
| `unions/` | — | **The union registry**, separated from the module pack: the 111-hall taxonomy, its eight districts and the ten-campus network — three district campuses (Treasure Island–San Francisco, Oakland, New Orleans) plus Houston, Chicago, Seattle, Pittsburgh, Denver, Miami and Detroit, seven regional-chapter **hub** campuses with no home district of their own — the ten-campus network target this pack tracked is now fully met — a builder with a source stamp, and a verifier that proves the module pack still agrees with the roster |
| `pack/` | ACP-10 | Module registry at 111-hall scale: 25,875 authored skeleton objects (consuming the union roster), a consumer library that generates all 11,000,000 module IDs from them, and a verifier that proves uniqueness over the whole population rather than a sample |
| `control/` | ACP-02/03/04/05/06/07/15 | The deterministic control plane — profile, dial, affect, hint ladder, skill graph, sequencer, assessment gates — with two simulation harnesses |
| `fabric/` | ACP-11/12 | Mentor contract and middleware, the five-gate eval harness with adversarial stubs, supervisor/swarm router |
| `security/` | — | Deny-by-default authorization with tenant isolation, rate limiting, data classification, retention and erasure; the contest and abuse routes (a learner reaches a human, never an agent); the CycloneDX SBOM of the vendored browser libraries (`security/registry/sbom.cdx.json`), held to `web/vendor/` by hash on every run; plus `SECURITY.md` with the threat model and the launch checklist as an honest status table |
| `ops/` | ACP-13 | The three registries with a pipeline transition table, hall-by-hall rollout lanes that ask ACP-08 rather than re-deciding (an audit log is required to run one at all), the seven-job automation loop, the cost governor that cannot throttle the control plane, and a wave 1 rehearsal that drives every lane gate against seeded synthetic cohorts with a full audit trail — evidence the machinery works, not that a wave has shipped to a real hall |
| `web/` | — | The published pages and their builders: landing page, rendered protocol spec, the languages page, the campus plan with a generated floor plan for every hall, the **network geomap** (`trade_craft_geomap.html` — the geo registry on a real WGS84 map with vendored MapLibre GL: RECORDED campuses, anchors and city frames, DERIVED great-circle routes, provenance in every popup, no basemap tiles unless asked for, and on request the public-domain USGS orthoimagery plus the city's own building footprints fetched live from its authority and extruded), the **interactive layered map** (districts, pipeline, module layers, training stations, the district tool crib on every hall panel and floor plan, all locales, searchable and deep-linkable) and the **3D environment** (the ten-campus network as a 3D board — enter a campus city, then any hall for its extruded floor plan, fixtures, station beacons and first-person walk mode; each campus carries its own atmosphere — sky, fog, drifting fog banks over Treasure Island, a synthesized ambient bed of wind, gulls, harbor horns, insects and thunder (no recordings), and a day/night/storm cycle derived from the same records: crushed sky and moonlight after dark, grey light, thicker fog and falling rain in the storm — at twice the campus radius of earlier rounds — districts sit further from the plaza, the ring road and green grew with them, the ground and its water dressing widened to clear it, and the perspective camera's far plane was corrected to clear both the campus and the region board's fog-far (a pre-existing mismatch that used to pop geometry at the clip plane before fog hid it) — optimized to a measured budget — identical geometry shared through one cache and building decoration pooled into one mesh per material per district, station beacons instanced, roads and lane dashes merged per campus (campus scene: ~997 → ≤530 → 208 draw calls on the 51-hall flagship, at double the earlier radius; 268 since the campus gained a walkable **training yard** — a fenced apron on the green with a stand for every simulator seat the campus's own halls teach, each stand paved in that seat's declared floor, walked up to and entered on foot, and drawn only where a campus has home halls, since a hub's chapter seats are a roster of trades and never a place), the region board built once and every view disposed rather than detached on rebuild (a region→campus→hall→walk→sim→restoration→region cycle no longer grows the heap), the hall/finish/avatar payload deduped on the wire and inflated at boot (3D page 660 → 432 KB, interactive map 264 → 138 KB), and an adaptive quality ladder that steps resolution and shadows down when the frame rate stays under budget — the authority's public-domain orthoimagery laid under the city layer on request, georeferenced off the same campus record the RECORDED anchors use, plus a live campus minimap that rings fully-worked halls, campus-wide completion rollups in the HUD, the learner's own avatar riding the training machines, and the district tool crib hung and clickable in every hall's tools room; experimental WebXR entry — VR and AR buttons that appear only where the platform offers the session — portable saves: the device-local record exported and imported as JSON by the learner alone; and the metaverse layer's doors — the avatar and any hall exported as named glTF 2.0 binaries, and the learner's own .glb rendered on the locker's guest stand), and the **network dashboard** (`trade_craft_dashboard.html`) — every platform figure read live from its own registry, the 10/10 progress meter for the now fully-met ten-campus target, and both provenance tiers shown side by side rather than blurred together) |
| `brand/` | — | The canonical identity: names and forbidden spellings with reasons, the two-theme token set, the wordmark, hall livery, and a lint that fails the build on drift |
| `bus/` | ACP-01/08/09 | The message bus with single-writer enforcement, the telemetry envelope and its quality guards, the append-only audit log, the parity/stop-condition/override safeguards, and the whole loop assembled over them |
| `console/` | — | **The Adaptive Console**: a single-file web app running the real protocol on the real pack, with the builder and its staleness guard |
| `i18n/` | — | **The locale catalogs**: the Academy's vocabulary in 8 languages with structural-parity and pack-agreement checks; a closed `translation_status` set and a reviewer-attribution rule (a catalog claiming review names its reviewer and a date; a hall-name catalog — `i18n/hall-names/<locale>.json`, none shipped yet — is complete across all 111 halls or absent, never partial), enforced by the suite and again at build time (`i18n/validate.mjs`); the landing, campus-map and languages page generators now read every user-visible string from here (`i18n/lint_hardcoded.mjs` holds them to zero of their own); hall names deliberately untranslated pending native review; all 7 non-English catalogs remain machine-drafted, pending native review — nothing here became `reviewed` |
| `wiki/` | — | **The wiki**, generated from the registries: a page per map (campus, interiors, skill graph, languages) and a page per district, with Mermaid content graphs and a staleness guard |
| `stations/` | — | **The station registry**: the recovered pre-rebrand yard curriculum rebranded onto the live structure — 25 machine-gradable stations assigned to halls, skills and floor-plan rooms, verified against all three |
| `surfaces/` | §24 | **The surface registry**: 22 floor finishes with renderer-ready parameters and the reason each exists, resolved per room hazard-first with §24.1's discipline, plus per-room conditions (illuminance, air changes, design noise, temperature band, PPE) merged more-demanding-wins across every governing hazard — rendered as the 3D room floors and read out live in walk mode |
| `geo/` | — | **The geo registry**: real WGS84 coordinates for all ten campuses (Oakland's RECORDED from the Locator.X city table, cross-checked on build; Houston's, Chicago's, Seattle's, Pittsburgh's, Denver's, Miami's and Detroit's AUTHORED, the same weaker tier a roadmap candidate carries), recomputed great-circle distances and bearings for all forty-five campus pairs, 47 city/institution anchors and city frames for every campus — 19 anchors and 3 frames RECORDED, copied from Locator.X's committed tables and sources for the three flagship campuses (cross-checked on build), 28 anchors and 7 frames AUTHORED for the seven hub campuses (Port of Houston Authority, University of Houston, San Jacinto College, Houston Community College; Chicago Union Station, Navy Pier, University of Illinois Chicago, Richard J. Daley College; Port of Seattle, University of Washington, Museum of Flight, South Seattle College; Port of Pittsburgh Commission, Carnegie Mellon University, Carrie Blast Furnaces National Historic Landmark, Community College of Allegheny County; Colorado School of Mines, National Renewable Energy Laboratory, Denver Union Station, Community College of Denver; PortMiami, NOAA Atlantic Oceanographic and Meteorological Laboratory, University of Miami, Miami Dade College; Detroit/Wayne County Port Authority, Wayne State University, The Henry Ford, Wayne County Community College District — real, named, widely-known institutions, never invented, typed from general knowledge the same way the hub campus centres themselves already are) — and Mapbox-ready GeoJSON, every anchor and frame carrying its own real provenance through to every surface that shows it (3D panel, hover label, in-world sign, geomap popup, advisor citation), never a hardcoded claim. The 3D network view places its plates by these true bearings and marks each anchor on the campus rim, and every campus grows a walkable city layer — New Orleans at true linear offsets, Treasure Island as an island in the Bay with ferry lines and Bay Bridge spans at true bearings, Oakland on its waterfront, Houston on the schematic Buffalo Bayou, Chicago on the schematic Chicago River and Lake Michigan, Seattle on the schematic Puget Sound, Pittsburgh on the schematic confluence of the Allegheny, Monongahela and Ohio rivers, Denver on the schematic South Platte River, Miami on schematic Biscayne Bay, Detroit on the schematic Detroit River (streets, bridges and water always schematic, labelled so); plus the on-foot bands a distance only means something against — 800 m for ten minutes, 1.2 km for fifteen, and the six destination classes a walkable measure has to count — RECORDED from Locator.X's walk module and cross-checked against it on build, carrying its caveats intact: not Walk Score®, straight-line rather than street-network, and honest that this bundle holds no shop records to count |
| `sims/` | — | **The simulator registry**: eleven operable training seats (tower-crane lift, excavator trench cut, forklift yard run, the weld bead bench — arc gap, travel and burn-through discipline — the scaffold bay build — sills to rails in the legal order, refusals counted — the rigging signal call, where the learner is the signalperson and the crane follows only correct calls — the load chart judgment, where the machine carries one honest chart and an overweight pick accepted is the failure that matters — the pressure washer surface clean, where a containment berm has to go down before the first spray or the containment axis fails outright — the airless paint sprayer finish, where too-close or too-slow passes run and drift past the masked line counts as overspray either way — the **boom lift basket work**, where the harness clips and the stabilizers set before the basket ever leaves the ground, the load moment stays under the rated line and the basket stays out of the overhead line's exclusion zone — and the **overhead crane shop move**, where the load travels bridge-then-trolley along the marked route, never over the pedestrian aisle or the workstation, above every obstacle, sway held low into the target square), each ringed by its five-point pre-shift walkaround — clipboards around the machine, a habit-builder the registry declares NOT a gate: nothing locks behind it and marking it changes no score with schematic physics in the 3D environment, deterministic rubrics, skill bindings proven against the roster and the graph, and a declared cockpit per machine — dash gauges with warn thresholds, synthesized audio (WebAudio, no recordings — the arc is filtered noise, the pump is a filtered drone), haptic cues, an operator-seat view mode (cab, driver, welding visor, scaffold deck, the signalperson's pad, the chart board, the wand or the gun, the lift basket, the pendant walk beside the load — each a seat POSE that lands on the camera on a desktop and on the XR rig in a headset), and per-region training scenarios (the campus picks the yard; the rubric never varies), plus an `xr` control mapping per seat (thumbsticks, trigger, grip and face buttons into the same key state a keyboard fills — declared once here, read by the page's controller adapter, verified against a mocked WebXR session only) — and a **scripted reference operator** per seat: a hand-written, deterministic control policy in the page, a function of the seat's own live gauges, the scenario's params, the seat's declared yard `layout` (supply pad, spoil zone, cell pitch — declared once here, read by the sim and the operator both) and a level, no model behind it and no network reached, exactly as the advisors are scripted. At `optimal` it passes every pass-gated rubric axis on all 33 seat-x-yard combinations (proven by a headless fixed-step sweep of the built page); `novice` and `hurried` are honestly-labelled, fixed-seed perturbations of the same procedure, so a sweep yields labelled sub-optimal episodes too. The registry owns its metadata — the closed level set, the axes each operator guarantees (asserted equal to the seat's pass-gated axes) and its ordered procedure, which the page's policy is a switch over, the Operator advisor quotes and the wiki prints. Its provenance word is **SCRIPTED**: not a learned policy, not real equipment, not a claim about any physical robot, and not AI-SYNTHESIZED (the word `orbis/` keeps for generated video). Not equipment certification, and the registry says so |
| `tools/` | — | **The toolroom registry**: one tool crib per district — twelve generic hand tools each with a schematic render shape, a hue and the one line of what it is for (no manufacturer or brand named or drawn) — hung on a pegboard in every hall's tools room in the 3D environment, plus the **crib drill**: a deterministic match-the-job-to-the-tool check derived from the crib itself (option order is index arithmetic, not chance), graded into the same device-local learner record as the simulators, with every hall bound through its district to a real `tools.applied` skill |
| `schools/` | — | **The schools pack**: the gamified flipped-classroom model (explore at home, build and practice in class, verify unaided — the teacher circulates), grade bands aligned with the Cognition.X vocabulary, 4 proposed school-district records across the three district-campus regions (public-record names only; every record states that no district has reviewed or agreed), and one live flipped unit per simulator-bound hall — every reference proven against the packs that own it. Live in the 3D app too: a 🎓 Schools panel renders the model, bands and district records, every flipped unit clicks straight through to the hall that runs it, and a hall that runs a unit links straight back — both directions proven against the same page |
| `avatars/` | — | **The avatar pack**: a humanoid locker — 18 standard sections at 15–20 options each (build, skin, hair style + colour, eyes, facial hair, headwear from hard hats to ball caps, tops, vest, trousers, footwear, tool belts, outerwear, extras, costumes — 336 options) plus the **crew section seating all 111 halls** (111 looks, so 447 options in 19 sections), each stamping its three-letter-code mark in the district hue on vest, shirt and headwear (the Academy's own insignia — no real union's logo is drawn), 22 one-tap **character** presets (each a full outfit with a line of story, validated against the locker), 19 themed **costumes** from the Mardi Gras krewe to the foundry heat suit — including five original animal mascots — plus **SmartCiti.X TradeApes**: an original collection of 111 apes, one per hall, generated deterministically from the roster (free, cosmetic only, NOT tokens — no NFT, no sale, no blockchain — and imitating no third-party ape artwork; anatomy holds to a measurements-only ape reference recorded in the registry — span, hunch and material roles measured from a user-supplied scan whose mesh is never shipped), and 12 emotes with emoji and procedural moves on a capsule-built humanoid with real facial features; **cosmetic only** — every option free and unlocked, none read by any grader, and the suite asserts it |
| `parcels/` | — | **The city-records registry**: the source contract for mimicking the three district-campus regions from their own records — the authority, dataset, licence, field schema and exact bounded query for each region's parcel and building-footprint GIS (New Orleans' `data.nola.gov`, the SF Assessor roll via DataSF, Alameda County's assessor FeatureServer; 254,122 records published upstream), all RECORDED from Locator.X's committed builders and cross-checked against that checkout — Houston, Chicago, Seattle, Pittsburgh, Denver, Miami and Detroit, seven hub campuses with no district ring to overlay footprints under, carry no records contract of their own, and the registry says so — plus the public-domain USGS orthoimagery both maps draw, plus the **real USGS 3DEP ground elevation** (the Elevation Point Query Service, adopted point-for-point from Locator.X's own lookup) — one coordinate at a time, on demand, never a bulk pull: open any RECORDED anchor's card and a button fetches the real elevation at that exact spot, guarding the same three traps the real service is known to throw. **No record is copied here**: the maps fetch from the authority in the learner's own browser and fall back to the SCHEMATIC layers — or say the lookup failed — when a source does not answer |
| `meta/` | — | **The metaverse-layer registry**: the interchange contract, built to the open baseline (no private standard is referenced or claimed) — glTF 2.0 export of the avatar (named rig) and any hall (named rooms) that Unity, Sketchfab, Blender, Godot and three.js open directly; learner-local .glb import onto the locker's guest stand (never uploaded, the asset's own licence stays the learner's to honour); WebXR, GeoJSON and OGC GeoPose 1.0 (emitted by `spatial/`) as the other three claimed standards — the WebXR role now stating exactly what the 3D environment does in a session (an XR rig the headset rides in, `local-floor` with a `local` fallback, AR passthrough, controller input into every simulator, snap-turn locomotion, a wrist panel of readouts, the scripted operator watchable in-session) and what it does not (hand tracking, rendered hands, and any run on a physical headset: verified against a mocked WebXR session in headless Chromium only) — with the OMBI spatial-fabric, SOM and RMAP shapes honestly listed as NOT claimed beside VRM, OMI extensions and USD; a recorded review of three MIT-licensed Unity avatar systems (UniVRM, Microsoft Rocketbox, Ready Player Me) read from their own checkouts, with VRM's humanoid bone vocabulary ADOPTED for the exported rig — which now carries **every bone VRM requires of a humanoid** (15 required plus chest and neck) as real nested transform nodes, so a Unity Humanoid or VRM import retargets it without a hand-built avatar definition, and the bones are driven rather than decorative: a distance-keyed walk cycle with knees that bend only one way, counter-swinging elbows, a breathing idle and a head that turns toward the viewer inside a human range — no animation clip is exported, only the rest-pose skeleton — while VRM, OMI extensions and USD stay honestly listed as NOT claimed; the org's [`ml-agents`](https://github.com/AGIFutureFoundation/ml-agents) fork (Unity ML-Agents Toolkit, Apache-2.0) RECORDED as the Unity-side training consumer — export-ready, with no trained agent claimed until one exists there; and a suite that holds every claim against the page source that implements it |
| `agents/` | — | **The advisor registry**: nine scripted guides — orientation guide at the door, safety steward, crib keeper, layout hand, inspector, foreman, records clerk, and the dispatcher on the campus green, plus **the Operator**, who stands inside a simulator's own yard rather than a hall room — the Academy's own rigged avatars, each answering a closed list of 35 questions between them. Twenty-one of those topics carry no words at all: they are BINDINGS the page resolves against the record that already holds the fact, so the safety steward's answer is the hall's own condition record and changes when the hall does — one truth per fact survives the advisor being added. The Operator's seven bindings are the newest of these - the seventh, `seat.procedure`, quotes the scripted reference operator's own step list from the simulator registry, the list the page's policy is written around - and fix a real gap the other room-bound advisors had: a hall training more than one machine (e.g. `crane-ops` trains crane-lift, rigging-signals *and* load-chart) used to surface only its first-bound seat's rubric and walkaround through the Inspector and Foreman; the Operator instead resolves against whichever seat is ACTUALLY running, live, so every machine gets its own correct answer. The other fourteen topics are written down and each names the file it was written in. An advisor is **not an instructor and not an AI**: no model runs behind one, nothing is generated at view time, no network is reached, it cannot be asked an open question, it represents no real worker or union officer, and talking to one changes no score — the suite asserts the last by reading the graders |
| `world/` | — | **The world registry**: the sky, the weather it is seen under, the ground it stands on and the animals moving through it. **There is no texture file in this repository and none is downloaded** — every surface is a recipe (base colour, grain, octave count, relief depth) and the page generates the colour map *and its normal map* in the learner's browser at boot, which is why the render contains no third-party artwork; the suite reads the built page and fails if it so much as mentions a texture loader or an image extension. Six weather states (clear, overcast, fog, rain, storm, night) on one button, each a set of **multipliers over whichever campus atmosphere is loaded** rather than a look of its own, so a campus keeps its character in the rain; rain is a rate, not a switch. An equirectangular sky dome with the sun drawn where the key light actually comes from, a value-noise cloud band, and a seeded star field — a night sky, not *the* night sky. And 101 animals across all ten campuses — gulls on the Bay and the Seattle waterfront, pelicans and egrets on the Gulf coast and Biscayne Bay (New Orleans, Houston and Miami alike), a ring-billed gull on the Chicago lakefront, pigeons and a yard dog everywhere including Detroit — each with a written reason to be in the yard, declared as ambience and not a wildlife survey, and absent entirely for viewers who ask for reduced motion |
| `labels/` | — | **The label registry**: every word in the 3D world is on a sign, and a sign should be readable *before* it is read. 19 kinds over 15 shapes — **shape carries the category** (a speech bubble is somebody who will talk to you, a tab with a pointer is a place you can enter, a pin on a stem is somewhere real out there, a chip is something you can open, a readout is a number a machine measured), **colour carries provenance and district** (a hall wears its district's own hue, a RECORDED place a solid accent, a SCHEMATIC one a dashed outline that restates the claim the registry already makes and never upgrades it), and **type carries rank** (display for names, sans for the line under them, mono for anything measured). Every sign also reads the view: each frame it is scored on the angle from the view direction inside a 34° cone and on distance *relative to how far out the view is*, and the score drives opacity, size and tint, eased so nothing flickers as the head turns. The single most centred sign is the **focus** — tinted and lifted, so a learner can see what they are about to act on without a cursor, which is what makes it work in a headset. On screen a sign is clamped between 3.4% and 8.5% of the viewport's height, so it never dominates what it labels nor shrinks out of legibility. Presentation only: no label is a score, none gates anything, and the suite proves no grader reads which sign a learner faced |
| `training/` | — | **The training-data registry**: every interaction already produces a structured fact - a sim run ends in a measured rubric outcome, an advisor answer is a fixed topic against a fixed record, a walkaround check is a point marked done - and this pack declares the shape those facts are recorded in when a learner chooses to keep them, episode by episode, device-local under its own `tc-training` key so it can never collide with `tc-progress`. Shaped to hand to the org's own [`ml-agents`](https://github.com/AGIFutureFoundation/ml-agents) fork (already RECORDED in `meta/`) as example JSON a conversion script could read — not a native Unity ML-Agents `.demo` file, and exporting it trains nothing by itself. Every episode is SCHEMATIC (deterministic rubrics, not a real robot), anonymous (no name, no email, no device id), on by default with a visible toggle that never touches episodes already kept, and — the property that matters — strictly downstream of a score already final: the suite proves every rubric's pass/fail expression is computed with no reference to training state at all, so the recorder can never influence what it records. **TRACE**, its own off-by-default add-on with its own toggle, folds a coarse (~1 Hz, capped) sample of a running sim's own `gauges()` readout — the same numbers the dashboard already shows, nothing computed anew — into that episode's `outcome.trace`; still not a per-tick physics or joint trajectory, since these are schematic single-machine simulators, not articulated robots. A sim episode now names its **actor** — `human` (every episode as it always was) or `scripted-reference`, the scripted reference operator `sims/` declares, whose episodes also carry `operator: {level, seed, scenario, steps, dt}`, the handle that replays them identically under the records panel's headless fixed-step sweep (every seat x every yard at a chosen level, one click, through the SAME `simResults -> recordEpisode` path, the same toggle and the same rolling cap — and never the learner's own progress record, which the page guards by name). Their provenance word is SCRIPTED, declared by `sims/`, cited here, never AI-SYNTHESIZED. The records panel now names its pairing with `orbis/` in the panel itself, with a button straight there — and the Orbis panel links back the same way, so a learner discovers both halves of the same `ml-agents` story from either one |
| `roadmap/` | — | **The network roadmap**: the path from zero built campuses to ten walkable worlds, tracked as a target rather than claimed as an achievement — and, as of this build, a target that is fully MET: all ten of the built entries are the union registry's own campuses; three are district campuses, RECORDED or DERIVED and cross-checked exactly as `geo/` already does, and seven — Houston, Chicago, Seattle, Pittsburgh, Denver, Miami and Detroit — are **hub** campuses: no home district, no district ring, and a regional-chapter seat for all 111 halls instead, on **AUTHORED** coordinates like a candidate's used to carry. Detroit, the last candidate — a real US metro with a real trade profile (a century of heavy manufacturing) — was the tenth and final promotion, so `CANDIDATES` in `roadmap/build.py` is now empty by design: this build does not invent an eleventh campus or a new "next" candidate to keep the roadmap open. The registry still carries the actual checklist (source real coordinates, cross-check them, register the campus, build the 3D geometry, give it a world, verify and ship) that every one of the ten built campuses cleared in full, the record of how the network got here rather than a queue with anything left in it. The dashboard, the 3D region board and the network geomap all render the zero-candidates case honestly — a flat "0 candidates" count and an empty marker set, never a dangling list — rather than assuming a candidate always exists |
| `orbis/` | — | **The Orbis synthetic-training prompt contract**, in two parts. First, a text-only bridge the static site itself ships: a deterministic prompt for every one of the 111 union modules, built for Reactor's hosted `visko-orbis-stable` video model — the page builds the text locally and calls no network host, ever; the command sequence, auth shape and model id are RECORDED from the cited starter README, `verified_from_build: false` because nothing here reaches that host to check it live. Second, two real, separately-runnable Next.js companion apps — `orbis/runner-helios/` (Helios, video only) and `orbis/runner-visko-orbis-stable/` (the same model MODEL above names, with real audio and resolution tiers) — each scaffolded from Reactor's own `create-reactor-app` templates (Apache-2.0), each with a scene library generated by this pack from the same union registry (one truth, two consumers) and an optional pacing-adaptation panel (`FlowPanel`, backed by a pure, unit-tested `flow.ts`), that an operator installs and runs *themselves*, with their own `REACTOR_API_KEY`, never installed or started by this repo's own build or verify pipeline — and named, by path and model, from inside the static site's own text-prompt panel, so the two halves of this pack are no longer strangers to each other. Either way, a generated clip is AI-SYNTHESIZED synthetic video: not footage of any real trade, worker or site, and a separate-but-complementary stream from `training/`'s real, schematic episode log — both target the same `ml-agents` fork, neither claims to be the other |
| `guide/` | — | **The helper guide**: the in-app agent you reach with one click from anywhere — ten places (the six world views and the four panels), each answering the same six questions (what this is, what you can do, how to move, how to get back, what is measured, what it does not claim) from written answers that each cite the file they came from. SCRIPTED: a fixed set of places and a fixed set of questions, with no model behind it and nothing that accepts free text, because nothing behind it could answer free text. The control tables are read rather than written — the 42 seat rows come from `sims/registry/sims.json` verbatim, and a running seat resolves its own rows at the moment you ask. Voice is two switches, both off until you turn them on and both disclosed at the toggle: reading an answer aloud uses `speechSynthesis` and prefers a voice that speaks on your machine, saying so when the only one available is a network voice; asking by voice uses the browser’s own `SpeechRecognition`, **which in Chrome, Edge and Safari sends the recorded audio to a speech service run by that browser’s vendor** — this bundle neither operates nor can see that service, stores no audio and keeps no transcript, asks Chrome to recognise on the device where that is offered, and stops rather than falling back when the language pack is missing. Four hand gestures over the 25 WebXR hand joints (pinch to select, point to move, open palm to stop, palm up to open the guide), each with a hysteresis band and a dwell; every distance is AUTHORED and the pack says so — no headset has been available, and the recogniser is proven against synthetic joint positions fed straight through it, never against a real hand |
| `sky/` | — | **The day-night sky**, and the page runs it. Fourteen phases across a solar day, ten of them solved by bisection from the elevation that defines the phase rather than placed by eye; sun elevation and azimuth DERIVED from standard solar-position formulae at the flagship campus’s own latitude, read from `geo/`; per-weather sky treatments for the six states `world/` already declares, held as pointers into that registry so not one of its numbers is copied; five ordered draw layers with predicates stating when each renders; and a reproducible star field regenerated from the page’s own seed. Colours are AUTHORED and say so. The pack records what it found in the page it is meant to drive: the sun is one fixed vector at 49.98° elevation shared by all ten campuses at every hour; `night` is modelled as a *weather state* rather than a time, so a rainy night is unreachable; and stars are keyed off weather rather than off the sun, so no state but `night` can ever show one. All three are now fixed in the page: the sun is placed by the selected hour, a weather state that is really a time says so through `phase_override` instead of the page inferring it, and the stars are up when the sun is down and merely dimmed by cloud. What is **not** wired, and the pack says so in its own honesty block: the 56 per-phase gradient stops, the tint, the horizon haze and the sun disc — the page still composes its gradient from the campus’s own stops darkened by the weather. The hour is stepped, never interpolated, because `setSky()` rebuilds the environment map on every call |
| `lessons/` | — | **Walkable lessons**: a reason to go to a particular room in a particular hall and do something there that is taught, sequenced and recorded. 32 lessons over 135 steps, spread deliberately across the trades — all 11 skill strands, 27 halls, no hall holding more than a declared and enforced share. Every step resolves to something that already exists: a station in `stations/`, a seat and scenario in `sims/`, a walkaround point, an advisor topic in `agents/`, a crew role, a toolcrib. Nothing new is invented to hold them — the steps that record write one of the four episode kinds `training/` already has, and the ones that do not carry an explicit null and say why. They plug into the `schools/` flipped-classroom loop at its `class` and `floor` stages and leave `home` and `gate` alone. AUTHORED, and the limit rides with the capability: no lesson certifies, qualifies or licenses anybody, and a clause-level scan stops the build on any sentence that claims otherwise without denying it in the same clause. Lesson content remains unverified general practice pending authoring by journey-level practitioners from the halls each lesson names |
| `restoration/` | — | **The Bay Restoration pack**: 11 real sites across two categories — nine real, independently-run San Francisco Bay habitat-restoration sites and programs (Heron's Head Park, Candlestick Point, the South Bay Salt Pond ecotone levee, Montezuma wetlands, American Canyon, the North Bay STRAW program, Alviso, an East Oakland youth program, and the bay-wide invasive-Spartina removal effort) — AUTHORED FROM PUBLIC RECORD (this build reaches no network host, so nothing here is fetched or cross-checked against sfbayrestore.org live), each linking to its own real source page and, where one exists, its own real workforce-development pathway (Eco-Apprentice, STRAW, Planting Justice's youth program) — none of them a SmartCiti.X program; plus two environmental-monitoring sites: **Hunters Point Naval Shipyard**, a real, currently-litigated federal Superfund cleanup site in San Francisco's Bayview-Hunters Point neighborhood (Navy-led under CERCLA, EPA Region 9 and California DTSC oversight) — its facts are a September 2026 snapshot of public record (a settled $57M Tetra Tech data-fraud settlement, and Greenaction's still-open 2024 lawsuit against the Navy over cleanup adequacy, each independently cited, never conflated), and it points to the real, community-run Marie Harrison Bayview Air Monitoring Project (Greenaction / IVAN BVHP) rather than any invented SmartCiti.X program or workforce pathway; and **Former Naval Station Treasure Island (NSTI)** — the island this bundle's own flagship campus scene sits on — a real Navy BRAC/CERCLA cleanup (DTSC the lead regulator, CDPH for radiological matters, the Regional Water Board for water quality) that is explicitly NOT NPL-listed (its EPA Superfund ID CA7170023330 is an assessment record; the NPL-listed "Treasure Island Naval Station–Hunters Point Annex", CA1170090087, is the Hunters Point site, and the entry says so in its own `disambiguation` note), its 21 facts a 17 September 2026 snapshot (1993 BRAC listing and 1997 closure, parcel-by-parcel transfer to TIDA under a 2009 conveyance agreement, Site 12's unresolved Ra-226 criteria with a draft Record of Decision not scheduled before 2027, and a resident class action DISMISSED in 2022 — never called settled), pointing to the real Navy-convened Restoration Advisory Board as oversight participation and to One Treasure Island's Construction Training Program as the island's real union pre-apprenticeship — which its own note says is not part of the cleanup and not a SmartCiti.X program. Every one of the 11 sites also names its real `trade_needs` — real union slugs from `unions/registry/unions.json` whose own registered focus text actually matches that site's field work (never forced onto a site it doesn't fit). Three field-skill tracks bridge the habitat-restoration field work to real skill_ids this bundle's own graph already ships (grounds, site-safety, laborers, surveyors, survey-drone) — no restoration-only skill is invented. Live on the network geomap (10 sites mapped at their own real coordinate, visually distinct from campuses, roadmap candidates and from each other by category), on the network dashboard (site / track count and its own honesty line), and in its own 🌊 3D-app panel, where every field-skill track clicks straight through to the hall that actually teaches it — and any hall that teaches one links straight back, badge and all, the same bidirectional pattern proven for Schools and Orbis. Eight of the ten mapped sites — the habitat-restoration ones — also stand as real, walkable markers in the Treasure Island / Oakland city layer, and each opens its own standalone ground-level scene — schematic terrain composed from that exact site's own real habitat/scale/org record, never a shared template, honestly captioned as schematic rather than surveyed — with three in-world field-skill-track beacons a learner can walk up to and click straight through to the real hall. Hunters Point and NSTI are explicitly `walkable: False`: each is pinned and clickable on the map with its own flat facts-and-citations panel — the same flat-panel pattern the elevation/aerial-imagery lookups already use elsewhere in this pack — but neither ever renders as a walk-through ground scene, because an active federal cleanup site — litigated, or with unresolved radiological criteria — is not something this platform can responsibly present as a place to stroll; the walkable Treasure Island campus scene is a schematic training campus at the island's derived centroid, and the NSTI entry is the real-world record of the ground it stands on |
| `spatial/` | — | **The spatial-fabric pack**: the Academy published as a self-hosted *spatial fabric* in the sense of the Metaverse Standards Forum's Open Metaverse Browser Initiative (OMBI) — static files a plain web server serves next to the site, no server, no RMAP endpoint, no network, no DID minted. **Claimed outright:** OGC GeoPose 1.0 (OGC 21-056r11, `application/geopose+json`) — one Basic-YPR pose for every campus, institution anchor and pinned restoration site (67: 10 / 47 / 10; the bay-wide Spartina program is honestly unposed because its registry pins no coordinate), the horizontal position read at build time from `geo/` or `restoration/` at that registry's own provenance (RECORDED / DERIVED / AUTHORED) and asserted equal to it on every build, while height and heading — which this bundle does not hold for anywhere — are emitted as `0` with an explicit UNKNOWN sidecar beside every pose, never invented. **Honestly NOT claimed:** the OMBI fabric manifest (`fabric.json` — places, on-demand `.glb` doors, in-page services, external origins), the Scene Object Model (`som.json` — a multi-origin scene graph, one branch per origin, per-branch ownership asserted) and RMAP; their normative texts were not reachable from this build, so both files are *shaped* after the public deck and press (Q3 2026), registered in `meta/` under `not_claimed` with the reason, and have not been loaded in Sneeze, Artemis or any other metaverse browser. Every other operator this fabric points at — a restoration site's own organization, the Navy / EPA / DTSC at Hunters Point, the Navy / DTSC / CDPH at Former Naval Station Treasure Island, a port authority, a university, a neighbouring city — is a separate external origin that serves nothing through this fabric, the same not-affiliated line the rest of the bundle holds; the 111 halls' real organizations are deliberately not listed as origins at all. On the network dashboard with a download of both files, and on its own wiki page |

Alongside the packs: [`ROADMAP.md`](ROADMAP.md) — the phased plan from v3.2
forward — and [`wiki/Home.md`](wiki/Home.md), the index of every map's page
(37 generated pages, staleness-guarded). `.github/workflows/pages.yml`
publishes the committed, verified pages — the landing page, every map and
environment under `web/`, and the wiki — to GitHub Pages on every push to
`main`; the workflow deploys, it does not build, so Pages can never show a
page the suite has not seen.

### Simulation harnesses

```bash
cd control
node simulate.mjs          # ACP-14: the dial vs fixed/random, six learner archetypes
node sweep.mjs             # dial parameter sweep (window x loopGain)
node sim_curriculum.mjs    # ACP-15: graph sequencing vs blocked/flat curricula
node sweep_workingset.mjs  # how many skills should be open at once
```

## Seventeen defects found by simulation

Every one of these was in the protocol, not merely in code written against it.
They are listed because the pattern matters: each was invisible to inspection
and obvious to a harness. Defects 8–9 needed thousands of attempts to appear;
10–11 needed hostile inputs rather than time; 12–13 only appeared when the
packs were assembled and run from somewhere other than where they were written;
14–15 needed two modules to be read against each other, since each half was
reasonable alone; 16 needed the thing to be rendered and measured, because it
was an assertion that agreed with itself and not with the browser.
All the harnesses ship alongside the unit suites, because each finds a class the
others structurally cannot.

| # | Defect | Found by |
|---|---|---|
| 1 | The session rail froze the dial — a never-re-anchored clamp pinned it ±10 from where a learner started | shadow-mode sim |
| 2 | Two clamp sites with different anchors let the setpoint escape the session cap | shadow-mode sim |
| 3 | Practice could never certify anyone: the dial serves the band centre, the gate accepts only the band top | curriculum sim |
| 4 | Improvement retroactively invalidated its own evidence — rising θ disqualified past demonstrations | curriculum sim |
| 5 | Asking whether a gate passed could silently revoke it | curriculum sim |
| 6 | Mastery belief outran evidence — 33/33 skills read as mastered after 201 attempts | curriculum sim |
| 7 | Unbounded interleaving is thrashing — a wide frontier learned 0.67 skills against 6+ | curriculum sim |
| 8 | The setpoint could escape the theta safety rail when a stale session anchor outlived a falling theta | soak |
| 9 | The verification budget deadlocked the sequencer — stalled at ~2,200 attempts with 25 of 33 skills uncertified | soak |
| 10 | One non-finite difficulty set theta to NaN and permanently destroyed a learner's profile | fuzz |
| 11 | The console staleness guard reported "current" while the page shipped a control plane two fixes behind | fuzz |
| 12 | The staleness guard's own source list was a second copy, and drifted the moment a new pack was added | integration |
| 13 | Suites passed only from the directory they were written in — the registry and console paths resolved against the working directory, so the packaged bundle's own verification could not run | packaging |
| 14 | Scaffold fading never fired anywhere: the ceiling was stamped only on verification picks and the mentor's fallback filled the gap with the *full* ladder, so every practice task ran unfaded | integration |
| 15 | The eval gate on mentor promotion stood in front of a mutable record — a caller could flip `evals.pass` on the object it had just been handed | adversarial |
| 16 | A layout assertion was built on a guessed px-per-character constant that was 43% low; it passed while four hall names overflowed their pads | rendering |
| 17 | Four published surfaces stated three different network sizes, and nothing compared them; the figures lint now reads the canonical counts from the verified pack | cross-surface |

## What has and has not been verified

**Verified here:** the ledger closes at exactly 11,000,000 with every ID proved unique;
the dial holds the ZPD ~6× better than fixed or random difficulty; the graph
sequencer is the only strategy that certifies anyone, and roughly doubles
durable ability against blocked practice; the eval harness fails each
adversarial mentor on exactly its own gate; the router's recursion guard
terminates handoff cycles.

**Not verified, and flagged in place:** `fabric/langgraph_reference.py` has
never been executed. Both simulations encode pedagogical assumptions
(prerequisite gating, forgetting curves, the cost of over-helping) and test
whether the system converts them into outcomes — they produce no evidence that
the assumptions are true. The affect signals in the dial simulation were
generated by the same model that consumed them, which is circular. The
working-set sweep result partly contradicts the interleaving rationale in
ACP-05, and the model is under-specified for that question — see ACP-15 §16.4.
Lesson titles and library labels in the registry are placeholder strings.

Shadow mode on real telemetry — logging intended actions and acting on none —
remains a required gate before activation.


## The console

`console/trade_craft_console.html` is the protocol wired to a screen — the tested
modules inlined verbatim, task content from the registry pack, every adaptation
showing its reason. Rebuild it with `python3 console/build_app.py` after changing
any protocol module. Its README lists the two integration bugs building it caught.

## v2.0 changes

Rebranded from the former working name to **SmartCiti.X : Trade Craft Academy
(powered by AGI Corp)** across every file, filename, identifier and generated
artifact — the registry and ledger were regenerated rather than patched, so no
stale brand survives in data.

Optimization pass in the same regeneration:

- **Registry payload roughly halved**, 12 MB to 6.6 MB. `union`, `level`, `slot`
  and `track` are derivable from a lesson id and its shard header, and the
  five-option check is identical on every row — writing them 1,221,000 times bought
  nothing. Flags now appear only when true. Key names stayed readable, because
  authors hand-edit these files and 2 MB is not worth a cryptic schema.
  `registry.js` hydrates the omitted fields on load, so consumers still receive
  complete lesson objects and no calling code changed.
- Dead imports and unreachable constants removed across all packs.
- Superseded bundle copies and scratch files dropped from the tree.

All checks pass against the regenerated data.

## v3.2 changes

- **Spec updated to v3.2** — §23 (what an adversarial review found: thirty-three
  defects, nine fail-open safeguards, the general finding that author-written
  tests agree with author-written code) and §24 (texture and environment
  packages) — and the version unified across the manifest, the registry and
  every generated surface.
- **Unions separated from modules.** The taxonomy now lives in `unions/` with
  its own registry, builder and source stamp; `pack/` consumes the built
  roster, and `unions/verify.mjs` proves the two packs agree instead of
  letting one directory imply it.
- **Eight languages.** `i18n/` carries the locale catalogs (English source +
  Spanish, French, German, Portuguese, Chinese, Hindi, Arabic), each held to
  structural parity and pack agreement by `i18n/test.mjs`, rendered
  direction-aware by `web/trade_craft_languages.html`. Hall names are
  deliberately untranslated pending native review, and every catalog says so.
- **A wiki and a roadmap.** `wiki/` is generated from the registries — a page
  per map with Mermaid content graphs, a page per district with every hall's
  figures — with a staleness guard in `verify_all.sh`. `ROADMAP.md` schedules
  v3.3 → v4.0 with harness-checkable exit criteria.
- **Packaged-layout path defects fixed.** `web/build_map.py`,
  `web/build_page.py` and the console slice builder resolved paths that only
  worked in the pre-packaging tree (defect 13's shape); all now walk up to
  the pack root. The duplicate `web/build_slice.py` was removed, and the
  orphaned 33-hall-era map data moved to `archive/` with its provenance
  recorded in `wiki/Provenance.md`.

## Running it as an app

The bundle is a static site: `index.html` at the root is the hub, and every
surface under `web/` and `console/` is a self-contained page. Deploy it on
any free static host, no build step required:

- **Vercel** — import the GitHub repository (framework preset: *Other*, no
  build command, output directory: repository root). `vercel.json` is
  already configured.
- **GitHub Pages** — `.github/workflows/pages.yml` already deploys on every
  push to `main` via `actions/configure-pages`, which requires the
  repository's Pages source to be set to **GitHub Actions**, not the legacy
  branch-deploy mode: Settings → Pages → Build and deployment → Source →
  **GitHub Actions**. This is a one-time step a repo admin has to take by
  hand — the workflow cannot enable Pages for itself — and until it is
  done every run of `pages.yml` fails at the `configure-pages` step with
  `Get Pages site failed … Not Found`, not at the deploy step, so the
  built site is never stale — it simply is not live yet.
- **Netlify** — drag the repository folder onto the drop zone, or connect
  the repo with no build command.
- **Locally** — `python3 -m http.server` from the repository root and open
  `http://localhost:8000/`. (The 3D environment needs an HTTP origin —
  browsers refuse ES-module imports from `file://` — so use the local
  server rather than double-clicking the file. Three.js 0.160.0 is
  vendored under `web/vendor/`, so every page works offline.)
